/**
 * Re-export property address utilities from the linking module.
 * These are shared across both systems.
 */
export { parseAddress, resolveRead, resolveWrite } from '../linking/PropertyAddress';
