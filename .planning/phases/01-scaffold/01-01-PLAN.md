---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .eslintrc.json
  - .prettierrc
  - vitest.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/engine/.eslintrc.json
  - src/engine/core/types.ts
  - src/engine/core/index.ts
  - src/engine/cell/index.ts
  - src/engine/rule/index.ts
  - src/engine/preset/index.ts
  - src/engine/worker/index.ts
  - src/renderer/.gitkeep
  - src/store/.gitkeep
  - src/commands/.gitkeep
  - src/components/hud/.gitkeep
  - src/components/terminal/.gitkeep
  - src/components/viewport/.gitkeep
  - src/components/panels/.gitkeep
  - src/ai/.gitkeep
  - src/lib/index.ts
autonomous: true
requirements: [FOUN-01, FOUN-02, FOUN-04]
user_setup: []

must_haves:
  truths:
    - "pnpm dev starts the Next.js dev server without errors"
    - "pnpm lint passes with zero errors under TypeScript strict mode"
    - "pnpm tsc --noEmit passes with zero type errors"
    - "Engine directory has zero UI imports enforced by ESLint rule"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "src/engine/core/types.ts"
      provides: "Core engine type definitions"
      min_lines: 10
    - path: "src/engine/.eslintrc.json"
      provides: "Engine isolation lint rule"
      contains: "no-restricted-imports"
  key_links:
    - from: "src/engine/core/types.ts"
      to: "src/engine/core/index.ts"
      via: "re-export"
      pattern: "export.*from.*types"
---

<objective>
Scaffold the Next.js project with TypeScript strict mode, ESLint, Prettier, Tailwind CSS, and the full folder structure per spec.

Purpose: Establish the project foundation with correct tooling, strict TypeScript, and engine isolation enforced by lint rules from day one.
Output: A working Next.js app with `pnpm dev`, `pnpm lint`, and `pnpm tsc --noEmit` all passing. Folder structure matching the architecture spec with engine isolation lint rule.
</objective>

<execution_context>
@/Users/lf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-CONTEXT.md
@.planning/phases/01-scaffold/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with pnpm</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, .eslintrc.json, .prettierrc</files>
  <action>
  Initialize the Next.js 16 project in the current directory using `pnpm create next-app@latest . --typescript --tailwind --app --turbopack --eslint --use-pnpm`. If the directory already has files, handle by creating in a temp dir and moving files, or use the --yes flag.

  After scaffolding:
  1. Ensure `tsconfig.json` has `"strict": true` (Next.js should set this by default)
  2. Add `"skipLibCheck": true` to tsconfig for yaml/Zod compatibility
  3. Configure `next.config.ts` with webpack WASM experiment:
     ```typescript
     const nextConfig = {
       webpack: (config) => {
         config.experiments = { ...config.experiments, asyncWebAssembly: true, layers: true };
         config.output.webassemblyModuleFilename = 'static/wasm/[modulehash].wasm';
         return config;
       },
     };
     ```
  4. Add Prettier config (`.prettierrc`) with standard settings
  5. Install dev dependencies: `pnpm add -D vitest @vitejs/plugin-react jsdom prettier`
  6. Install core dependencies: `pnpm add three zustand yaml zod`
  7. Install type dependencies: `pnpm add -D @types/three`
  8. Create `vitest.config.ts` with jsdom environment for Three.js tests
  9. Add scripts to package.json:
     - `"test": "vitest run --dir src --dir engine"`
     - `"test:watch": "vitest --dir src --dir engine"`
     - `"typecheck": "tsc --noEmit"`

  Clean up the default page.tsx to a minimal "Lattice" landing page.
  </action>
  <verify>pnpm dev starts without errors (check with timeout); pnpm lint passes; pnpm tsc --noEmit passes</verify>
  <done>Next.js project running with TypeScript strict mode, Tailwind CSS v4, ESLint, Prettier, and Vitest configured</done>
</task>

<task type="auto">
  <name>Task 2: Create folder structure and engine isolation</name>
  <files>src/engine/, src/renderer/, src/store/, src/commands/, src/components/, src/ai/, src/lib/, src/engine/.eslintrc.json, src/engine/core/types.ts</files>
  <action>
  Create the full directory structure per the architecture spec:

  ```
  src/
    engine/
      core/       -> types.ts, index.ts
      cell/       -> index.ts (stub)
      rule/       -> index.ts (stub)
      preset/     -> index.ts (stub)
      worker/     -> index.ts (stub)
    renderer/     -> .gitkeep
    store/        -> .gitkeep
    commands/     -> .gitkeep
    components/
      hud/        -> .gitkeep
      terminal/   -> .gitkeep
      viewport/   -> .gitkeep
      panels/     -> .gitkeep
    ai/           -> .gitkeep
    lib/          -> index.ts
  ```

  Create `src/engine/.eslintrc.json` with the engine isolation rule:
  ```json
  {
    "rules": {
      "no-restricted-imports": ["error", {
        "patterns": [
          { "group": ["react", "react-dom", "react/*", "react-dom/*"], "message": "Engine must not import React" },
          { "group": ["next", "next/*"], "message": "Engine must not import Next.js" },
          { "group": ["three", "three/*", "@react-three/*"], "message": "Engine must not import Three.js" },
          { "group": ["zustand", "zustand/*"], "message": "Engine must not import Zustand" }
        ]
      }]
    }
  }
  ```

  Create `src/engine/core/types.ts` with initial type stubs:
  - `WorkerMessageType` discriminated union for worker communication
  - `SimulationConfig` placeholder interface
  - `GridDimensions` interface (width, height, depth optional)

  Create index.ts files in each engine subdirectory that re-export from their modules (empty for now, establishes the import pattern).

  IMPORTANT: The engine directory must have ZERO imports from react, next, three, or zustand. The lint rule enforces this.
  </action>
  <verify>pnpm lint passes (including engine isolation rule); pnpm tsc --noEmit passes; all directories exist</verify>
  <done>Full folder structure created with engine isolation lint rule enforced. All TypeScript files compile. Engine has zero UI imports.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm dev` starts Next.js dev server
- [ ] `pnpm lint` passes with zero errors
- [ ] `pnpm tsc --noEmit` passes with zero errors
- [ ] All spec directories exist: engine/core, engine/cell, engine/rule, engine/preset, engine/worker, renderer, store, commands, components/hud, components/terminal, components/viewport, components/panels, ai, lib
- [ ] Engine isolation lint rule exists and would catch a react import in src/engine/
- [ ] `vitest.config.ts` exists and is valid
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- pnpm dev, lint, and typecheck all succeed
- Engine isolation lint rule is active and enforced
- Folder structure matches architecture spec
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`
</output>
